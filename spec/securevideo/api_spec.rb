require 'spec_helper'

describe Securevideo::Api do
  it 'has a version number' do
    expect(Securevideo::Api::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
